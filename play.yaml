---
- name: test ansible connection
  hosts: localhost
  connection: localhost
  become: false
  gather_facts: false
  tasks:
  - name: create resource group
    azure_rm_resourcegroup:
      name: rg_az204
      location: westus
